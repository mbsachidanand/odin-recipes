# odin-recipes
A website with a main index page which will have links to a few recipes.

It is not expected look very pretty. The purpose is to build HTML chops.

In the future, it will be styled up with CSS.